#pragma once

namespace constants{
    const float HP = 5.08f;
    const float JACKYSPACE = 7.5f;
    const float JACKNTXT = 11.5f;   // Jack + text
    const float JACKYSTART = 30.0f;
    const float Y_POSITION_CONNECT_LIGHT = 10.0f;
    static const int NUM_CHANNELS = 16;
};